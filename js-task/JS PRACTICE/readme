; ⚙️ FUNCTION PRACTICE QUESTIONS

; Write a function greetUser that takes a name and prints a greeting.
; 🔹 Hint: function declaration + parameter + string template

; Create a function add that returns the sum of two numbers.
; 🔹 Hint: parameters + return statement

; Write a function checkAge(age) that prints "Adult" if age ≥ 18, else "Minor".
; 🔹 Hint: if-else + parameters

; Make a function multiply(a, b = 5) that multiplies two numbers but has a default value for b.
; 🔹 Hint: default parameter

; Write a function sumAll(...nums) that adds any number of arguments passed.
; 🔹 Hint: rest parameters + loop or reduce

; Create a function that greets a user object: Hello, Irshad (20 years old)

; const user = { name: "Irshad", age: 20 };


; 🔹 Hint: destructuring in parameters

; Show difference between function declaration and function expression.
; 🔹 Hint: hoisting behavior

; Convert a regular function to an arrow function.
; 🔹 Hint: one-liner arrow syntax

; Write a function isEven(num) that returns true if even.
; 🔹 Hint: modulo operator + return

; Create a function sayHello that runs immediately when defined.
; 🔹 Hint: IIFE syntax

; 🧩 ARRAY PRACTICE QUESTIONS

; Given [2, 4, 6, 8], create a new array with each number doubled.
; 🔹 Hint: map()

; From [10, 15, 20, 25, 30], filter out numbers less than 20.
; 🔹 Hint: filter()

; Find the largest number in [1, 22, 3, 99, 4].
; 🔹 Hint: reduce()

; Count how many odd numbers exist in [1, 2, 3, 4, 5, 6].
; 🔹 Hint: filter().length

; Find total and average of [90, 80, 70, 60, 50].
; 🔹 Hint: reduce() + / array.length

; Extract first and second values from [100, 200, 300] into variables.
; 🔹 Hint: array destructuring

; Combine [1, 2, 3] and [4, 5] into one array.
; 🔹 Hint: spread operator ...

; Convert ['HTML', 'CSS', 'JS'] into 'HTML, CSS, JS'.
; 🔹 Hint: join()

; Sort names alphabetically: ['Riya', 'Irshad', 'Ayan'].
; 🔹 Hint: sort()

; Reverse array [1, 2, 3, 4, 5].
; 🔹 Hint: reverse()



; 🧠 OBJECT PRACTICE QUESTIONS

; Create an object car with brand, model, and start() method that logs <brand> started.
; 🔹 Hint: this keyword

; Add a new property to object and then delete it.
; 🔹 Hint: dot notation + delete

; Access nested property: student.subjects.js from object.
; 🔹 Hint: dot chaining

; Print all keys of an object.
; 🔹 Hint: Object.keys() or for...in

; Print all values of an object.
; 🔹 Hint: Object.values()

; Freeze an object and try modifying it.
; 🔹 Hint: Object.freeze()

; Seal an object and try adding/removing properties.
; 🔹 Hint: Object.seal()

; Destructure object {name: "Irshad", city: "Mumbai"} into separate variables.
; 🔹 Hint: object destructuring

; Given an array of objects, print names only.

; const users = [
;   { name: "Irshad", age: 20 },
;   { name: "Riya", age: 22 },
; ];


; 🔹 Hint: map() + dot access

; Combine two objects {a:1} and {b:2} into one.
; 🔹 Hint: spread operator or Object.assign()

; ⚔️ ADVANCED FUNCTION PRACTICE

; Write a recursive function for factorial.
; 🔹 Hint: function calling itself

; Recursive function to print Fibonacci sequence up to N terms.
; 🔹 Hint: return fib(n-1) + fib(n-2)

; Closure: make a counter function that remembers previous count.
; 🔹 Hint: inner function + lexical scope

; Write a higher-order function operate(a, b, fn) to apply different operations.
; 🔹 Hint: pass function as argument

; Callback: run one function after another finishes.
; 🔹 Hint: function as parameter

; Return a function from another function that greets by name.
; 🔹 Hint: nested function + closure

; IIFE that logs “Learning never stops!” immediately.
; 🔹 Hint: IIFE syntax

; Use setTimeout to print numbers 1–5 with 1-second delay each.
; 🔹 Hint: closure in loop problem

; Make a function that logs current time every 2 seconds.
; 🔹 Hint: setInterval() + clearInterval()

; Simulate a mini async chain using callbacks.

; startProcess(() => stepTwo(() => finish()));


; 🔹 Hint: nested callbacks



; 🟢 Level 1 – Basic Function, Array & Object Code
; Tasks (Easy)

; 1. Write a function `sayHello()` that prints `"Hello
; JavaScript"`.

; 2. Create a function `add(a, b)` that returns their sum
; and log the result.

; 3. Write a function with a default parameter `name =
; "Guest"` that prints `"Hi <name>"`.

; 4. Use rest parameters to make a function that adds
; unlimited numbers.

; 5. Create an IIFE that prints `"I run instantly!"`.

; 6. Make a nested function where the inner one prints a
; variable from the outer one.

; 7. Create an array of 5 fruits. Add one at the end and
; remove one from the beginning.

; 8. Use a `for` loop to print all elements of an array.

; 9. Create an object `person` with keys `name`, `age`,
; and `city`, and print each key’s value.

; 10. Use `setTimeout()` to log `"Time’s up!"` after 2
; seconds.




; Question Sheet

; 🟡 Level 2 – Functional Thinking & Logic Tasks
; (Intermediate)
; 1. Write a higher-order function `runTwice(fn)` that
; takes another function and executes it two times.

; 2. Create one pure function that always returns the
; same output for a given input, and one impure
; function using a global variable.

; 3. Write a function that uses object destructuring
; inside parameters to extract and print `name` and
; `age`.

; 4. Demonstrate the difference between normal
; function and arrow function when used as object
; methods (the `this` issue).

; 5. Given an array of numbers, use `map()` to create a
; new array where each number is squared.

; 6. Use `filter()` to get only even numbers from an
; array.

; 7. Use `reduce()` to find the total salary from an array
; of numbers `[1000, 2000, 3000]`.

; 8. Create an array of names and use `some()` and
; `every()` to test a condition (e.g., all names longer than
; 3 chars).

; 9. Create an object `user` and test the behavior of
; `Object.freeze()` and `Object.seal()` by
; adding/changing keys.

; 10. Create a nested object (`user → address → city`) and
; access the city name inside it.